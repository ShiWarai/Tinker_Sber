build/
out/
tmp/
.cmsis/
# .vscode/ — в репозитории только настройки Run/Debug (остальное — локальное или генерируется)
.vscode/*
!.vscode/
!.vscode/tasks.json.d/
!.vscode/tasks.json.d/*
!.vscode/launch.json
.clangd
DebugConfig/
RTE/*

# CMSIS-Toolbox generated metadata (пересоздаётся командой cbuild)
F407_FC.cbuild-idx.yml
F407_FC.cbuild-pack.yml

*.log
build_log.txt
JLinkLog.txt
JLinkSettings.ini
EventRecorderStub.scvd

# Временные/кэш-файлы инструментов
*.bak
*.tmp
*.cache

# Артефакт сборки (создаётся post-build: fromelf --bin)
*.bin
*.axf
*.hex
*.map

# Пользовательские/локальные файлы Keil uVision (не нужны в репозитории)
*.uvguix.*
*.uvoptx
*.uvopt
*.uvgui.user
*.saved_uv4
